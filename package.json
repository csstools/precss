{
  "name": "gh-pages",
  "private": true,
  "dependencies": {
    "dacoda": "^2.0.0",
    "postcss": "^5.0.10",
    "postcss-scss": "^0.1.3",
    "precss": "^1.3.0"
  },
  "scripts": {
    "build": "browserify index.source.js | uglifyjs -c > index.js",
    "watch": "watchify index.source.js -o index.js"
  }
}
